export const metadata = {
  title: 'Legal Notice | Upward',
  description: 'Corporate information and legal hub for Upward.',
}

export default function NoticeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
