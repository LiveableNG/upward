/**
 * The homepage UI is served as static HTML via middleware rewrite to /landing.html.
 * See src/_archive/homepage/ for the previous React implementation.
 */
export default function HomePage() {
  return null
}
