/**
 * @upward/shared-types
 *
 * This package is the single source of truth for:
 *  - DTOs (request/response shapes validated by Zod on the frontend)
 *  - Entity interfaces (mirrors Prisma model shapes, without DB-coupling)
 *  - Enums used on both frontend and backend
 *
 * No implementation code lives here — only types and interfaces.
 * Core types will be added module-by-module as each sprint begins.
 */

// ── Placeholder exports (to be expanded per sprint) ──────────────────────────
export type {} // keeps the module valid until real types are added
