services:
  postgres:
    image: postgres:15-alpine
    container_name: upward-postgres
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Oluwaseun123
      POSTGRES_DB: upward
    ports:
      - "6006:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # redis:
  #   image: redis:7-alpine
  #   container_name: upward-redis
  #   restart: always
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redisdata:/data

volumes:
  pgdata:
  redisdata:
